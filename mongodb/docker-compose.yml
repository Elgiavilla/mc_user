version: '3'
services:
  mongodb:
    image: mongo
    ports:
      - "27071:27072"
    volumes:
      - "mongodata:/data/db"
    networks:
      - network1

volumes:
  mongodata:

networks:
  network1: